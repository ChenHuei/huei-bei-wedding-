export const PROJECT_TITLE = 'HUEI & BEI'

// enum

export enum SectionNameNouns {
  story = 'story',
  details = 'details',
  map = 'map',
  event = 'event',
}

export enum ColorModeNouns {
  system = 'system',
  light = 'light',
  dark = 'dark',
}
