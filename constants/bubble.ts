export const BUBBLE_PATH_LENGTH = 4

export const BUBBLE_DELAY = 0.5

export enum BubbleSizeNouns {
  max = 50,
  min = 20,
}

export enum BubbleDurationNouns {
  min = 8,
  max = 12,
}

export enum BubbleScaleNouns {
  min = 1,
  max = 3,
}

export enum BubbleOffsetNouns {
  min = -70,
  max = 70,
}
